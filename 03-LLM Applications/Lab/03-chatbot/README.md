# LLM Level-2 Starter Repo

This repo contains a Streamlit toy UI (Recipe Generator, Study Buddy, History Bot) and a Chainlit chat app (Summarize, Classify, Generate names).
All LLM calls use Gemini via `langchain_google_genai`.

See .env.example for required environment variables and run instructions.

